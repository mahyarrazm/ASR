numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
matplotlib>=3.9
lightgbm>=4.5
catboost>=1.2
xgboost>=2.1
shap>=0.46

# Optional. The pipeline runs without them and reports which candidates were
# skipped; TabPFN weights additionally require accepting the Prior Labs licence.
tabpfn>=2.0
pytorch-tabnet2>=4.6
torch>=2.0
